[tox]
envlist = mypy, py39, py310
isolated_build = True

[gh-actions]
python =
    3.9: py39, mypy
    3.10: py310

[testenv]
deps =
    pytest
commands =
    pytest {posargs}

[testenv:mypy]
deps =
    mypy
commands =
    mypy timezones {posargs:--ignore-missing-imports}
